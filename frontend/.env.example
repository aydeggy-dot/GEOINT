# Mapbox API Token
# REQUIRED: Get your free token from https://account.mapbox.com/access-tokens/
# Steps to get token:
# 1. Create free account at https://account.mapbox.com/
# 2. Go to Access Tokens page
# 3. Copy your default public token (starts with 'pk.')
# 4. Paste it below and restart the dev server
#
# NOTE: Without a valid token, the map will not display
VITE_MAPBOX_TOKEN=your_mapbox_public_token_here

# API Base URL (default uses Vite proxy)
VITE_API_URL=/api/v1
